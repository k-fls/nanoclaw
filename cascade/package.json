{
  "name": "@fls-claw/cascade",
  "version": "0.0.0",
  "private": true,
  "description": "Automerge tool for fls-claw: branch model, ownership, versioning.",
  "type": "module",
  "scripts": {
    "cascade": "tsx scripts/cli.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "ignore": "^5.3.2",
    "yaml": "^2.6.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=20"
  }
}
