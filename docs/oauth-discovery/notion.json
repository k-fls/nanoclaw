{
  "issuer": "https://api.notion.com",
  "authorization_endpoint": "https://api.notion.com/v1/oauth/authorize",
  "token_endpoint": "https://api.notion.com/v1/oauth/token",
  "api_base_url": "https://api.notion.com/v1",
  "_source": "manually researched from official docs (developers.notion.com/docs/authorization)",
  "_note": "Notion OAuth2 for public integrations. Token endpoint handles both authorization_code and refresh_token grant types. Token requests require HTTP Basic Authentication with client_id:client_secret (base64 encoded). The owner parameter in the authorization request must be set to 'user'."
}
